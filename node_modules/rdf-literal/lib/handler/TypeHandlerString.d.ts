import * as RDF from "rdf-js";
import { IToRdfOptions, ITypeHandler } from "../ITypeHandler";
/**
 * Translates strings.
 */
export declare class TypeHandlerString implements ITypeHandler {
    static readonly TYPES: string[];
    fromRdf(literal: RDF.Literal): any;
    toRdf(value: any, { datatype, dataFactory }: IToRdfOptions): RDF.Literal;
}
